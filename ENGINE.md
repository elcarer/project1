# Документация движка (engine)

ECS-движок 2D-игры на **PixiJS 8.19** без сборщика и зависимостей (кроме самой PixiJS, подключённой локально).
Дата последнего обновления: 2026-09-13 (dualgrid: слой объектов + вкладки в редакторе). Бэкап кода до первого рефакторинга: `../engine-backup-before-refactor/`.

## Как запустить

**Игра (открытый мир + персонаж)** — открыть `index.html` двойным кликом (работает
с `file://`) или по адресу `http://127.0.0.1:8137/index.html` на сервере. Параметры:
`index.html?w=500&h=500&seed=7` — размер и сид мира. Управление: WASD/стрелки/джойстик —
движение, атака автоматическая (враг в зоне оружия), P — пауза, колесо — зум.

Для разработки удобнее статический HTTP-сервер с запретом кэша:

```bash
python nocache-server.py      # отдаёт Cache-Control: no-cache, порт по умолчанию 8137
python nocache-server.py 8791 # порт можно передать аргументом
# открыть http://127.0.0.1:8137/ (или указанный порт)
```

⚠️ Обычный `python -m http.server` не шлёт заголовков кэширования — браузер может отдавать
скрипты из эвристического кэша, не обращаясь к серверу (симптом: правки не применяются).
Лечение: nocache-server либо Ctrl+F5 / cache-buster в адресе.

Весь движок подключён классическими `<script>`-тегами в dual-mode (файлы без
`import/export`, глобали через `globalThis` — см. «Модули»), поэтому страницы работают
и с `file://`. Для ассетов игры на `file://` есть вшитая копия `scripts/embedded_assets.js`
(тайлсеты + оборотень; по http не используется — живые файлы с диска).

**Пересборка ассетов после правок PNG/JSON в `images/`** — один скрипт на всё:
`python images/rebuild_registry.py` (сухой прогон — `--check`). Он проверяет все
запечённые места и обновляет только изменившиеся: реестр объектов
(`images/objects/objects_data.js`), вшитые ассеты игры для file://
(`scripts/embedded_assets.js` — тайлсеты + wolf_64 + 27 персонажей forWork) и вшитые тайлсеты редактора
карт (`EMBEDDED_TILESETS` в `dualgrid_editor.html`).

## Структура файлов

```
engine/
├── index.html                  # входная точка: подключает pixi.min.js и game.js (type="module")
├── editor.html                 # ПИКСЕЛЬНЫЙ РЕДАКТОР: персонажи 64×64, кадры анимации, экспорт
├── dualgrid_editor.html        # РЕДАКТОР КАРТ: слои, кисть/штамп, выделение, генерация, save/load JSON
├── dll/pixi.min.js             # PixiJS 8.19 (локальная копия, включает AnimatedSprite)
├── nocache-server.py           # сервер разработки: no-cache заголовки + POST /save (сохранение PNG)
├── images/
│   ├── bullets/all.png         # спрайтшит пуль 4×32px кадра (используется)
│   ├── sprites/knight_64.png   # рыцарь 64×64, 4 кадра (нарисован в редакторе)
│   ├── objects/                # СЛОЙ ОБЪЕКТОВ dualgrid: 633 декора (деревья, кусты,
│   │   │                       # постройки, геммы, снег…) — PNG кратны 32px, якорь низ-центр
│   │   ├── objects.json        # справочник: имя/категория/размер/вес (для движка)
│   │   └── objects_data.js     # те же объекты data-URL (WebP) — реестр редактора
│   └── tiles/
│       ├── grass_dirt.png      # dual-grid тайлсет 4×4 «трава-земля» (32px тайлы)
│       ├── grass_water.png     # dual-grid тайлсет 4×4 «трава-вода» (32px тайлы)
│       ├── snow_dirt.png       # dual-grid тайлсет 4×4 «земля-снег» — фон земля, фича снег
│       └── sand_dirt.png       # dual-grid тайлсет 4×4 «земля-песок» — фон земля, фича песок
└── scripts/
    ├── game.js                 # ИГРОВАЯ ЛОГИКА + полигон модулей (движок здесь только вызывается)
    ├── editor.js               # логика пиксельного редактора (window.__EDITOR — API для агента)
    ├── cut_sheet7.py           # append-only вырезка нового листа объектов (сегментация + альфа + дописывание реестра)
    ├── data/units.js           # UNIT_CONFIGS (конфиги типов юнитов) + UNITS_EVENTS (константы событий)
    └── engine/
        ├── engine.js           # ядро: ECS, хранилища, системы, пулы, фабрики спавна, API
        ├── eventSystem.js      # шина событий on/off/emit/once/clear (подключена, экспортируется ядром как events)
        └── modules/            # ОПЦИОНАЛЬНЫЕ МОДУЛИ — игра подключает нужные
            ├── math.js         # чистая математика: clamp/lerp/damp/rand/dist/angleTo
            ├── scheduler.js    # таймеры: after/every/update, кадронезависимо, замораживаются на паузе
            ├── pool.js         # дженерик-пул объектов (factory + onRelease)
            ├── input.js        # клавиатура+мышь: isDown/wasPressed/axis/pointer, endFrame
            ├── health.js       # компоненты hp/maxHp, damage/heal/kill, события unit:damaged/unit:died
            ├── camera.js       # следование, зум, тряска, границы мира, screenToWorld
            ├── fx.js           # частицы burst() и всплывающий text() через пул
            ├── audio.js        # Web Audio: синтез tone/noise, пресеты, unlock по жесту
            ├── assets.js       # загрузка с кэшем и nearest; нарезка кадров с PAD; loadCharacter (в т.ч. вшитый режим)
            ├── hud.js          # полоски (с авто-get) и тексты поверх мира, вне камеры
            ├── scenes.js       # конечный автомат состояний: add/go/is, хуки enter/exit/update
            ├── dualgrid.js     # генерация пола по тайловым картам: Dual Grid System, 4×4 тайла
            └── debug.js        # оверлей FPS/сущностей, визуализация сетки (G) и хитбоксов (H), F3
```

**Принцип разделения:** `engine.js` не содержит ни одного конкретного объекта — только
переиспользуемые механизмы. Всё, что относится к конкретной игре (какие текстуры, какие
типы юнитов, сколько и кого спавнить) — в `game.js`. Модули опциональны: ядро не знает
о них, каждый модуль подключается импортом и фабрикой в `game.js`.

## Ядро: ECS-подход

- **Сущность (Entity)** — просто числовой ID (`0..99999`). Никаких методов и полей.
- **Компонент (Component)** — чистые данные без логики.
- **Система (System)** — вся логика; работает только с группами-запросами.

### 1. Данные компонентов

Два хранилища (SoA — structure of arrays):

| Хранилище | Что лежит | Доступ |
|---|---|---|
| `COMPONENTS` | числовые данные в **типизированных массивах** | `COMPONENTS.positionX[id]` — O(1), кэш-дружелюбно |
| `DATA` | ссылочные данные: спрайты и т.п. | `DATA.spriteMap[id]` |

Компоненты ядра: `positionX/Y`, `velocityX/Y`, `configId`, `radius`, `gridCellId`,
анимационные `currentFrame/totalFrames/animationSpeed/animationTime` (кадры считает сам
AnimatedSprite, `animationSpeed` — маркер группы «animated»).

**Динамическая регистрация:** `ECS.registerComponent(name, type, length)` — модуль может
добавить свой компонент без правки ядра (так делает `health.js`: `hp` = бит 12, `maxHp` = бит 13).
Повторная регистрация идемпотентна. **Лимит: 32 компонента** (Uint32Array-маска), свободные
биты ищутся автоматически, при исчерпании — warning и `null`.

### 2. Группы-запросы (queries)

`ECS.createQuery(world, имя, [компоненты])` собирает битовую маску требований. Система
перебирает только плотный массив `world.queries.<имя>.entities`; попадание/удаление —
автоматически через `updateQueries` при изменении маски, удаление O(1) swap-and-pop.

Группы ядра: `movable`, `renderable`, `aliveUnits`, `animated`, `physics`; модуль health
добавляет `vulnerable` (зарегистрирован, используется игрой по необходимости).

### 3. Игровой цикл и системы модулей

```
app.ticker (каждый кадр):
  1. movementSystem     — movable:   позиция += скорость × dt (нормализация к 60 FPS),
                                     отскок от setWorldBounds() или краёв экрана
  2. spatialGridSystem  — physics:   очистка и перестройка пространственной сетки
  3. collisionSystem    — physics:   столкновения кругов, расталкивание + упругий отскок
  4. animationSystem    — animated:  sprite.update(ticker); скрытые вне вида камеры
                                     (visible=false от рендера прошлого кадра) пропускаются
  5. renderSystem       — renderable: спрайт.x/y = позиция + CULLING: всё вне вида камеры
                                     получает visible=false (PixiJS пропускает его целиком)
  6. СИСТЕМЫ МОДУЛЕЙ    — всё, что зарегистрировано через engine.addSystem(fn)
                          (камера, fx, hud, scenes, debug — в порядке подключения)
```

`addSystem(fn)` — точка расширения: ядро не знает числе систем модулей, вызывает каждый
кадр после своих. Скорости задаются в «пикселях за кадр при 60 FPS» — на 144 Гц движение
то же самое (проверено: 240 px/с при 143 FPS).

### 4. Сцена и контейнеры (PixiJS)

```
app.stage
├── worldContainer      ← МИРОВЫЕ координаты: им управляет камера (позиция/масштаб/тряска)
│   ├── частицы FX, визуализация отладки (сетка/хитбоксы)
│   ├── particleContainer (анимированные спрайты)
│   └── статические спрайты юнитов
├── hud.container       ← ЭКРАННЫЕ координаты: HUD, не зависит от камеры
├── debug.overlay       ← оверлей статистики
└── canvas
```

- **Пулы спрайтов.** Спрайты не создаются/не уничтожаются в рантайме: при смерти сущности
  спрайт прячется и возвращается в пул своего типа. Пулы **раздельные**:
  `STATIC_SPRITE_POOLS[typeIndex]` для `PIXI.Sprite` и `ANIMATED_SPRITE_POOLS[typeIndex]`
  для `PIXI.AnimatedSprite`. Прочие объекты (частицы, тексты FX) — через дженерик-пул `pool.js`.
- **Анимация.** `PIXI.AnimatedSprite(textures, false)`: 1 объект сцены на юнита,
  `autoUpdate=false` — кадрами управляет `animationSystem`.
  ⚠️ **Нюанс API:** в PixiJS 8.19 `AnimatedSprite.update(t)` ждёт **объект тикера** (читает
  `t.deltaTime`). Передача числа даёт NaN → спрайт исчезает из рендера.
- **Текстуры.** Программный атлас (`createProgrammaticSpritesheet`), загрузка спрайтшита
  (`loadSpritesheetFromImage` в ядре, `assets.loadSpritesheet` в модуле — с кэшем и прогрессом).
  Конфиг юнита поддерживает `spriteScale` (масштаб спрайта, применяется фабриками пула при
  создании и переиспользовании) — для уменьшения готового арта без перерисовки/нарезки.
- **Рендерер.** `app.init({ preference: "webgpu" })`: при недоступности WebGPU PixiJS сам
  откатывается на WebGL. Активный рендерер — `app.renderer.name` (выводится в консоль игры).

### 5. Пространственная сетка (SpatialHashGrid)

ДВУХУРОВНЕВАЯ сетка поверх `Map`. **Точный уровень** (16px, `cellSize` — адаптивный:
`max(16, 2×макс.радиус конфигов)`) хранит ID сущностей; каждый кадр `clear()` возвращает
массивы ячеек в пул `freeArrays` → `insert()` по `getCellKey(x,y)`. Ключ уникален для любых
координат (схема `(col+32768)*65536 + (row+32768)`) — коллизий ключей нет даже при
отрицательных координатах; обратный разбор — `getCellCoords(key)`.

**Грубый уровень** (`coarseSize: 64`, битовая карта занятости 1024×1024 блоков): при
переборе соседей позволяет пропустить точный Map.get, если весь блок 4×4 ячеек пуст
(`useCoarse`). По замерам ВЫКЛЮЧЕН по умолчанию: на плотной толпе все блоки заняты и
фильтр даёт +10% кадра, в разреженном — нейтрален; выигрыш возможен лишь при очень
низкой заполненности блоков. Оставлен как диагностическая опция.

`collisionSystem` обходит ЯЧЕЙКИ, а не сущности: для каждой ячейки берутся пары внутри
неё и пары с четырьмя «передними» соседями (право, низ-лево, низ, низ-право) — каждая
пара сущностей проверяется ровно один раз, обращения к карте — 5 на ячейку. Дистанция —
сравнение квадратов; физика пары вынесена в `resolveCollision(a, b)`.
Уменьшение точной ячейки 32→16px при радиусах 5–6 сократило число проверяемых пар
в ~4 раза (плотность на ячейку): −15…20% времени кадра на нагруженной сцене.

### 6. События (шина `events`)

Ядро и модули генерируют, игра подписывается (`events.on`):

| Событие | Когда | payload |
|---|---|---|
| `unit:spawned` | spawnUnit / spawnAnimatedUnit | `{ id, configId }` |
| `unit:damaged` | health.damage() | `{ id, amount, hp }` |
| `unit:died` | health.kill() (до удаления — позиция ещё читается) | `{ id, configId }` |

Константы имён — `UNITS_EVENTS` в `data/units.js`.

## Модули (подключаются опционально)

| Модуль | Фабрика | Ключевое API |
|---|---|---|
| math | функции | `clamp, lerp, damp, rand, randInt, dist, angleTo, TAU` |
| scheduler | `createScheduler()` | `after(sec,fn)→cancel, every(sec,fn)→cancel, update(deltaMS), clear()` |
| pool | `createPool(factory, onRelease)` | `acquire(), release(obj)`, счётчики createdTotal/activeCount |
| input | `createInput()` | `isDown(code), wasPressed(code), axis(), pointer{x,y,isDown,pressed,wheel}, endFrame()` |
| health | `createHealth()` | `attach(id,max), damage(id,n), heal(id,n), ratio(id), kill(id)` |
| camera | `createCamera({app, container, addSystem})` | `follow(obj), setBounds, zoomBy, shake(power,dur), screenToWorld` |
| fx | `createFX({app, layer, addSystem})` | `burst(x,y,opts), text(x,y,str,opts)` — всё через пул |
| audio | `createAudio()` | `unlock()` по жесту, `tone({...}), noise({...}), register(name,fn), play(name)` |
| assets | `createAssets()` | `load(urls, onProgress), loadTexture(url), loadSpritesheet(url,fw,fh), loadCharacter(base,{manifest,png}), textureFromDataURL(src), get(url), cache` — нарезка кадров с PAD-подрезкой UV |
| hud | `createHUD({app, addSystem})` | `bar(name,{get}) — автообновление, text(name,str), setText` |
| scenes | `createScenes({addSystem})` | `add(name,{enter,exit,update}), go(name), is(name), current` |
| character | `createCharacterInput()`, `createCharacterSystem({world,ECS,COMPONENTS,DATA,blocked,input,addSystem})` | `spawn({x,y,sprite,anims,…})→id, update(ticker), play/playAttack(id), facingName(id)` |
| tilemap | `createTilemapSystem({world,ECS,DATA,addSystem,container,Sprite,getView,mapW,mapH,ts,margin,tileAt,maxTiles})` | потоковый слой пола: `rebuild(), stats() → {tiles, parked, window}` |
| dualgrid | глобаль `createDualGrid()` (script-тег или import) | `build/update/tileIndex/detectLayout`, `generateMap({w,h,seed,frequency,scatter})`, `makeManifest/makeMap`, `mapFromManifest/mapFromJSON(json, textures)→Promise`, объекты: `generateObjects/buildObjects/syncObjects/placeObject/eraseObjectAt/objectAt/objectFootprint` |
| character | `createCharacterInput({target})`, `createCharacterSystem({world, ECS, COMPONENTS, DATA, blocked, blockedAlt, input, addSystem})` | `spawn({x,y,sprite,anims,speed,halfW,halfH,fps})→id, update(ticker), play(id,name), playAttack(id,aim?), getAim(id), remove(id), facingName(id)` — см. «Контроллер персонажа» |
| ai | `createEnemyAI({world, ECS, COMPONENTS, addSystem, characters, blocked, blockedAlt, getPlayerPos})` | FSM патруль→погоня→возврат: `register(id,{detectR,leashR,patrolR}), STATE (состояния врагов), STATE_NAMES` — см. «ИИ врагов» |
| combat | `createCombat({world, ECS, COMPONENTS, DATA, addSystem, characters, projectiles, fx, onRemove})` | ХП/урон/опыт/уровни: `init(id,{faction,hero,lvl,prim,growth,weaponMin,xpReward,size}), stat/dopOf/factionOf/alive, hpRatio/xpRatio, rollAttack(id), dealDamage(attacker,target,{base,critPower,melee}), heal, giveXp, revive` — см. «Боевая и ролевая система» |
| debug | `createDebug({app, addSystem, world, grid, components, layer, overlayPos})` | оверлей; `info[ключ]=значение`; клавиши F3/G/H |

Паттерн подключения (в `game.js`):

```js
const camera = createCamera({ app, container: engine.worldContainer, addSystem });
```

Все модули и ядро движка — **dual-mode**: файлы без `import/export`, фабрики публикуются
в `globalThis` (по образцу `dualgrid.js`). Подключаются обычными `<script src>` в
`index.html` (порядок важен: units → eventSystem → engine → math → остальные) — это
делает страницы рабочими с `file://`; как ES-модуль такой файл тоже валиден (глобаль
ставится побочным эффектом импорта). ⚠️ Верхний лексический уровень у классических
скриптов страницы ОБЩИЙ: дубликат `const имя` в двух файлах роняет второй файл целиком
(SilentError — просто нет глобали), поэтому хелперы объявлять внутри фабрик.

Модули, которым нужен тик (camera, fx, hud, scenes, debug), сами регистрируются через
`addSystem`. Планировщик и ввод — без авто-тика: `scheduler.update(ticker.deltaMS)` вызывается
в update сцены (поэтому на паузе таймеры стоят), `input.endFrame()` — в конце кадра сцены
(сброс однокадровых флагов после того, как сцена их прочитала).

## Генерация пола: Dual Grid System (modules/dualgrid.js + dualgrid_editor.html)

Автотайлинг по схеме Jess::Codes («двойная сетка»): тайл ложится не внутрь ячейки карты,
а на её угол — точку схождения 4 ячеек данных. Биты 4 ячеек (TL/TR/BL/BR) дают 2⁴ = 16
комбинаций — полный тайлсет всего 4×4 тайла (против 47 в классическом blob-автотайлинге),
а границы у местности получаются скруглёнными.

- **Карта** — `{ w, h, data }`, `data[y*w+x]` = 1 («фича»: земля/вода) или 0 (фон: трава).
  Слой пола занимает ровно `w*ts × h*ts` пикселей, тайл ячейки (i,j) выбирается по углам
  `D(i-1,j-1), D(i,j-1), D(i-1,j), D(i,j)`; за границами карты — значение `outside` (0).
- **build({texture, map, outside, hideBackground})** — собирает контейнер тайлов
  (нарезает тайлсет на 16 текстур, scaleMode «nearest»). **update(layer, map)** — подменяет
  карту с переиспользованием спрайтов (размер тот же) или пересобирает детей на месте.
- **Таблица углов TILE_CORNERS** — попиксельно выверена для `images/tiles/grass_dirt.png`
  и `grass_water.png` (раскладка у обоих одинаковая: тайл 6 — сплошная «фича», 12 — сплошной
  фон). Для чужого тайлсета 4×4: `detectLayout(текстура|изображение)` — автоопределение по
  цветам (фон = самый частый цвет, «фича» = самый далёкий от него из частых; окно 4×4 с
  отступом 2 у каждого угла голосует за ближайший эталон). Оба репозиторных тайлсета
  определяются точно (проверено в демо, «detectLayout 2/2»).
- **Наслаивание местностей** (река поверх пятен земли): верхний слой строится с
  `hideBackground: true` — его чисто-фоновые тайлы скрываются (у воды непрозрачная трава
  иначе закрасила бы нижний слой). Слои просто добавляются в один контейнер по порядку.
- **Подключение** — обычный `<script src="./scripts/engine/modules/dualgrid.js">` (как и
  pixi.min.js): модуль создаёт глобальную `createDualGrid()` и работает в том числе с
  `file://`. То же самое даёт `import "./dualgrid.js"` внутри ES-модуля (файл без
  `export` валиден как модуль, глобальная ставится как побочный эффект).
- **Редактор** `dualgrid_editor.html` (см. ниже) — рабочий инструмент для этих тайлсетов:
  генерация, рисование, сохранение/загрузка; старая демо-страница `dualgrid.html` удалена.

```js
const dual = createDualGrid();
const dirt  = dual.build({ texture: texGrassDirt,  map: dirtMap });
const water = dual.build({ texture: texGrassWater, map: waterMap, hideBackground: true });
worldContainer.addChild(dirt, water);
dual.update(dirt, newDirtMap);   // регенерация без пересоздания слоя
```

### Генератор и два формата файлов (по задумке геймдизайнера)

Два сценария, которые можно сочетать: **глобальную карту генерируют по манифесту**,
**локальные данжи отрисовывают из готового файла карты** (без генерации).

- **`generateMap({ w, h, seed, frequency, scatter })`** — процедурная карта `{w,h,data}`:
  fBm value-noise (3 октавы, детерминированный mulberry32; seed — число или строка).
  `frequency` — процент ячеек «фичи» (0..100), выдерживается **точно** порогом-квантилем;
  `scatter` — разброс: 0 — крупные материки (решётка 1.5 клетки), 100 — мелкие островки
  (до min(w,h)/2.2 клеток). Node-тесты: 30% → 30.01%; scatter 5 → 5–11 островков,
  scatter 95 → ~370 на 100×100.
- **Зазор между местностями (margin, default 3).** У тайлсетов без общего перехода
  (трава-земля ↔ трава-вода) стык «фича в фичу» рисуется прямыми срезами спрайтов.
  `mapFromManifest` при генерации отодвигает верхний слой от «фич» нижних слоёв с
  **другим** texture на `margin` клеток (дистанция Чебышёва ≥ margin+1, между ними
  остаётся полоса травы): `separateLayer(data, blockers, margin, w, h)` + `dilateMask`.
  Частота при этом выдерживается **после** раздвижки — если зазор съел часть слоя,
  входная частота детерминированно повышается до 8 итераций; если места всё равно нет
  (плотный нижний слой), слой получает максимум возможного (помогает меньший scatter
  или частота нижнего слоя). Слои с одинаковым texture считаются совместимыми и не
  раздвигаются. Node-тесты: дистанция после раздвижки ровно 4 при margin 3, дальние
  клетки уцелели, margin 0 ничего не трогает. Ловушка при стеке трёх и больше
  **разных** тайлсетов: blockers верхнего слоя — объединение всех нижних, и при
  плотностях по умолчанию (30%+25%) дилатация на 3 накрывает карту целиком — верхний
  слой молча остаётся пустым (компенсация частоты не спасает). Лечится уменьшением
  «зазора до нижних слоёв» у верхнего слоя до 0–1.
- **Привязка к фичам нижнего слоя (`on`, параметр слоя манифеста).** `on: "grass_dirt.png"`
  разрешает фичи слоя только внутри фич указанного нижнего слоя, с эрозией на 1 клетку
  (`erodeMask`) — переходные тайлы слоя лежат на его собственном фоне, а не на чужом.
  С привязанным слоем зазор не действует (их стык рисует его собственный тайлсет),
  от остальных нижних слоёв зазор сохраняется; «частота» считается по клеткам
  разрешённой зоны. Пример: снежные пятна (`snow_dirt.png`, фон земля) растут только
  на земляных пятнах `grass_dirt`, никогда на траве — стык снег↔земля гладкий из
  листа, а земля↔трава гладкая из `grass_dirt`. В редакторе — селект «поверх фич
  слоя» в панели активного слоя.
- **Манифест** (`format:"dualgrid-manifest"`): `{width, height, seed, layers:[{texture,
  frequency, scatter, outside, hideBackground, layout?, on?, png?}]}`. `makeManifest()` собирает
  с умолчаниями/проверкой; **`mapFromManifest(манифест, textures)`** генерирует и строит
  стек слоёв → `Promise<{root, layers, maps}>`. Сид слоя детерминирован:
  `hashSeed(`${seed}:${индекс}:${texture}`)` — движок и редактор дают идентичные карты
  (проверено двойной генерацией).
- **Файл карты** (`format:"dualgrid-map"`): `{width, height, layers:[{texture,
  data:[0/1…], outside, hideBackground, layout?}]}`. `makeMap()` собирает и валидирует;
  **`mapFromJSON(карта, textures)`** отрисовывает без генерации. Roundtrip проверен:
  сохранить → загрузить → данные совпадают побитово.
- **textures** — необязательный справочник `{ "имя.png": PIXI.Texture }` (ищется точное
  имя, затем базовое). Слой может нести `png: "data:image/png;base64,…"` — тогда файл
  самодостаточен (текстура грузится из самого JSON; редактор умеет вшивать их галкой
  «встроить PNG», загрузка из чистого реестра проверена).
- `layout` в файлах — таблица углов 16×4 (из `detectLayout`) для чужих тайлсетов;
  для репозиторных совпадает с `TILE_CORNERS` и может опускаться.

### Генератор мира: «🌍 Сгенерировать мир» (открытый мир для РПГ)

Классическая схема процедурной генерации миров: поля температуры/влажности
(широтный градиент + fBm-шум) задают вероятности местностей; точки интереса
расставляются разреженным поиском (упрощённый blue-noise / best-candidate).

- **`generateWorld({ w, h, seed })`** → `{ w, h, masks: { "grass_dirt.png", "grass_water.png",
  "snow_dirt.png", "sand_dirt.png" }, climate: { temp, moisture, snowZone, sandZone } }`.
  Температура `T = y/h + шум·0.36` → зоны `snowZone = clamp((0.40−T)/0.26)` и
  `sandZone = clamp((T−0.66)/0.26)`: волнистые границы поясов и карманы биомов в
  середине. Земляные пятна (субстрат снега/песка, привязка «on») гуще в поясах —
  0.14→0.76. Снег и песок — сумма ЗОННОЙ части (грубый шум, крупные поля) и
  РОССЫПИ редких пятен на мелком шуме (снег тает к пустыне, песок — к снегам):
  редкие проценты на грубом шуме выпадают кластерами в случайных местах, поэтому
  россыпь обязана жить на мелком шуме. Вода — по влажности, редеет в поясах
  (на юге редкие пруды = оазисы). Дальше обычный стек: эрозия земляных пятен и
  зазоры 2 между разными тайлсетами.
- **`rankNormalize(value)`** — ранговая нормализация (значение → доля среди всех,
  0..1 равномерно): порог `rank ≥ 1 − P(ячейка)` держит ЛОКАЛЬНУЮ частоту P.
  **`fbmField(w, h, rng, baseCells)`** — общая с `generateMap` fBm-поля.
- **`generateWorldObjects({ w, h, seed, masks, climate, items, tuning, noManMade })`** →
  `{ placements, pois }`;
  items — реестр (`{name, group, weight, cellsX, cellsY, pass?}`, порядок = индексы реестра).
  `tuning` (0..1, всё необязательно) — ручки густоты растительности: `coreDen`/`edgeDen` —
  плотность посадки в чаще/на опушке (дефолты 0.30/0.07; полог сажается на 20%/17% от
  них), `edgeT`/`coreT` — пороги лесного шума «где начинается опушка/чаща» (0.42/0.62,
  coreT прижимается вверх до edgeT+0.05). `noManMade` (дефолт **true**) — дикий мир:
  деревни не ставятся, из палитр амбиента выбрасываются следы активной человеческой
  деятельности (вывески, колодцы, бочки, мебель, топиары — префиксы
  `sign|banner|well|table_|easel|chalice|lamp_|pot_|topiary|birdhouse|barrel|crate|planter|fence|anvil|stall|notice|loom|spinning`);
  руины, костища и разбитые амфоры остаются.
  1) **POI** (кол-во от площади: деревни ~1/26 000, руины ~1/34 000, костища
     ~1/52 000 клеток): сайты с минимальной чебышёв-дистанцией (gap 24–34),
     деревни любят траву и середину карты, костища — пустыню. Кластер вокруг
     центра: деревня — колодец (север `well_bucket_snow`, юг/середина — колодцы,
     фонтан) + кольцо поддержки (север — снежные вывески/сугробы/бочки; юг/
     середина — заборы/навесы/указатели) + 1–2 ремесленных (кузница, станки);
     руины — врата/кромлех/башня + колонны/обломки; костище — `skull_cow`/
     `skeleton`/`bones_pile` + черепа/ямы/каирны. Вокруг POI — расчистка без
     амбиента. Палитры имён — `WORLD_OBJ_NAMES` (нет в реестре — пропускается).
  2) **Амбиент — растительные сообщества («лесные массивы», `WORLD_VEG`)**. В природе
     деревья одного вида растут патчами (ограничение разлёта семян), массив имеет
     доминанта и спутников, градиент плотности и поляны. Поля: `forest` (чаща F>0.62 /
     опушка 0.42–0.62 / луг), `stand` (шум mid-freq, квантуется в тип сообщества),
     `glade` (поляны в чаще), берега — ивы и камыш в 2 клетках от воды. Стенды по
     климатической фазе клетки: умеренная зона — дубрава / берёзовая роща / смешанный
     лес / хвойный лес; тайга (снежный пояс) — ельники / берёзняки-пионеры / сухостой;
     сухой пояс — ксерофиты / пальмовая роща / колючий скраб. У каждого стенда свой
     подлесок (у дубрав — дубовый лист и грибы, у хвойных — шишки, мхи, хвойный
     росток). Посадка ДВУХПРОХОДНАЯ: сначала полог крупных деревьев стенда (canopy,
     иначе дуб 8×8…10×11 клеток проигрывает footprint-конкуренцию мелким берёзам —
     было 3 дуба на карту, стало ~100), затем подрост/подлесок/кусты. Луг — травы и
     цветы, редкое одиночное дерево (`tree_oak_meadow` «Дуб на поляне»). Садовые и
     парковые растения (горшки, клумбы, топиар, сакура, бонсай) в дикой природе не
     встречаются. Снежный биом — группа snow, но только ВГЛУБЬ снега: снежные
     спрайты нарисованы под снежные тайлы и с кромкой не смотрятся, поэтому весь
     footprint объекта обязан лежать в эрозированной на 1 клетку маске снега
     (зазор ≥1 от края); холодные бесснежные клетки засеваются тайгой
     (ельники/берёзняки `vegZoneStands`), пустынный амбиент на жаркие бесснежные
     клетки остался как был.
     Вода, занятые клетки и расчистки POI пропускаются. Отдельный проход 3 —
     ВОДНЫЕ ОБЪЕКТЫ (группа `water`): ~2% водных клеток получают кувшинку/ряску/
     затопленную корягу; весь footprint обязан лежать на водных тайлах (спрайт —
     декаль со своей водяной основой); у `place` флаг `onWater` снимает запрет
     воды для опорных клеток.
     Посадка амбиента МЯГКАЯ: занятыми считаются только ОПОРНЫЕ клетки типа
     (клетки pass-сетки — ствол/основание; без pass — нижняя строка), поэтому кроны
     соседних деревьев свободно перекрываются и сплетаются в настоящую чащу —
     y-сортировка рисует ствол переднего дерева на фоне кроны заднего (жёсткая
     посадка всем footprint осталась у POI — постройки не врезаются друг в друга).
     ~83% объектов стоят в перекрытии с соседями. Луг сеется комками (клумбоватый
     шум: куртины трав/цветов 10% плотности, между ними голая земля 0.8%);
     универсальные кусты — почти только на опушке, в чаще подлесок своего стенда.
     Проверено: в блоках 10×10 клеток с ≥5 деревьями доминант (≥60%) в 99%.
- **Редактор:** кнопка «🌍 Сгенерировать мир» (размер из полей, сид из поля сида)
  применяет маски к слоям по именам тайлсетов и раскладку к объектам; результат —
  обычная карта: 💾 «Карта (JSON)» сохраняет всё, манифест мир НЕ воспроизводит
  (частоты там глобальные). `state.world.pois` — POI последней генерации;
  API: `__MAPEDITOR.generateWorld({ seed, w, h })` (fire-and-forget, статус в
  строке состояния), `applyView` для выставления вида из кода. Ползунки в шапке
  справа от «сетка данных» — ручки `tuning` (чаща/опушка %, пороги опушки/чащи,
  применяются «🌍 Сгенерировать мир»; порог чащи не опускается ниже опушки+0.05).
  Галочка «без объектов» (дефолт включена) — ручка `noManMade`: дикий мир без
  деревень и следов активной человеческой деятельности.
- Проверено (500×500): пояса монотонны на сидах 7/42/123 (снег 51–72% на севере →
  ≤0.3% на юге; песок зеркально), 22 POI, ~21 000 объектов (~83% в перекрытиях
  крон — чаща), 95% снежных объектов на севере, 100% костей на юге; self-test
  1 021 203/1 021 203 спрайтов; undo/redo на полном снимке 500×500 работает.
  Нижний предел зума снижен до 2% — иначе «⤢ вписать» не мог вписать 500×500
  (16000px мира в ~1000px окна).
  Внимание для Node-тестов: порядок items брать из `objects_data.js` (эталонный
  реестр движка/редактора), а не из `objects.json` — с индекса 405 порядки
  расходятся, от этого зависят индексы палитр и сид раскладки.

### Слой объектов (version 2, modules/dualgrid.js + images/objects/)

Декор поверх пола: 633 объекта с девяти листов арта elven_wood (деревья, кусты,
цветы, камни, постройки, мебель, геммы, руины, рытвины/скелеты, снежные объекты —
группа «snow», категория «Снег», водные объекты — группа «water», категория «Вода»
(41 шт: затопленные коряги и брёвна, ряска, кувшинки, водные камни, кораллы, камыш
на воде — лист 9, декали со своей водяной основой). Пайплайн —
APPEND-ONLY: существующие PNG и записи реестра никогда не пересоздаются
(ручные правки спрайтов не затираются), новые листы дописывает скрипт вида
`scripts/cut_sheet7.py` (сегментация компонент; ключинг белого фона с плавной
альфой и флудом от краёв; «еда белёсой кромки» волной; отсечение полуобрезанных
чужих обрывков у кромки; дедупликация повторов по сигнатуре; отбраковка объектов,
задевающих край исходного листа — визуально целые исключения в `EDGE_OK`;
слипшиеся объекты режутся по шву/сплит-прямоугольником по маске своего компонента).
Первые шесть листов вырезаны удалённым `scripts/cut_objects.py` (логика перенесена,
см. git-историю). Остатки фона за ключингом чистит `images/clean_halo.py`.
Лист 8 (кремовый фон) резал `scripts/cut_sheet8.py`: флуд по цветовой дистанции
от медианы кромки, un-blend от цвета фона, разрезка слипшихся через тени рисунков
по тёмным контурам с эрозией мостиков (тени прилипают к ближайшему контуру),
имена частей — `scripts/cut_sheet8_names.json`.

- **Каждый объект** — отдельная PNG-текстура с размером, кратным тайлу (32×32 …
  288×320). Якорь «нижний центр»: объект «стоит» в ячейке (x, y) основанием по её
  нижней грани, `footprint` занимает `cellsX×cellsY` ячеек (ячейка якоря — низ-центр,
  при чётной ширине объект нависает на клетку влево). Рендер — спрайты с
  `sortableChildren` и `zIndex = (y+1)·ts`: нижние по карте рисуются поверх верхних.
- **Генерация** `generateObjects({ w, h, seed, density, items, avoid }) → [[t,x,y]…]`:
  `density` — процент ячеек, получающих объект; `weight` каждого объекта —
  относительный шанс при выборе (0 — никогда); `avoid` — маска-запрет (в редакторе и
  `mapFromManifest` это «фичи» слоёв с `hideBackground`, т.е. вода). На ячейку ровно
  два вызова RNG → поток стабилен: смена весов не двигает раскладку (меняется только
  выбор объектов), смена плотности — только порог. Footprint объектов при генерации
  не пересекается и должен помещаться в карту. Сид потока: `` `${seed}:objects` ``.
- **API**: `buildObjects({items, ts, w, h})` → контейнер (`objectsMeta`); `syncObjects`
  (полная пересборка), `placeObject`/`eraseObjectAt`/`objectAt` (инкрементально,
  «ластик» берёт верхний объект, чей footprint накрывает ячейку).
- **Файлы v2**: манифест несёт `objects: {density, avoidHideBg, items:[{name, weight}]}`
  (раскладку движок генерирует сам, `mapFromManifest` строит слой объектов над полем);
  файл карты — `objects: {items:[{name, weight}], placements:[[t,x,y]…]}` (готовая
  раскладка, `mapFromJSON` отрисовывает без генерации). `version: 2`, файлы v1
  (без `objects`) читаются как раньше. Имена объектов — имена файлов без расширения;
  текстуры ищутся через тот же справочник `textures`/встроенный `png`, что и тайлсеты.
- **Реестр редактора**: `images/objects/objects_data.js` (подключён `<script>`-тегом,
  как и модуль — работает на `file://`): тот же список + data-URL WebP-lossless
  (P-квантизация PNG портит полупрозрачность tRNS, поэтому WebP). Для игр на http
  текстуры грузятся обычными путями из `images/objects/`. Редактор читает ТОЛЬКО
  data-URL: после ручных правок PNG пересоберите реестр —
  `python images/rebuild_registry.py` (это ЕДИНЫЙ пересборщик: помимо реестра
  обновляет `scripts/embedded_assets.js` и `EMBEDDED_TILESETS` редактора карт —
  все места, куда файлы из `images/` вшиты в код; перекодирует PNG в data-URL,
  метаданные сохраняет; `--check` — только отчёт о различиях; записи, чей PNG удалён
  с диска, вычёркиваются по умолчанию, `--noprune` оставляет их со старым
  data-URL — тогда в движке на http текстура по пути окажется битой).
- **Чистка фона**: кеинг листов оставляет по контуру мат (полупрозрачная
  светло-серая каёмка вокруг чёрной обводки) и кремовые карманы фона в
  промежутках между частями объекта. Чистит `python images/clean_halo.py`
  (`--apply` — записать PNG и пересобрать реестр; без него — только отчёт):
  волновое удаление от прозрачного края — нейтральная светлая полупрозрачность
  и почти-цвет-фона непрозрачные пикселы; тёмная полупрозрачность (тень) и
  насыщенное свечение не трогаются, дизайн за обводкой недостижим.
- **Проходимость (для коллизий персонажей)**: у каждого объекта сетка клеток
  cellsX×cellsY (32px), где 1 — непроходимо; по умолчанию непроходима нижняя строка
  (корни/ствол/основание), объект 1×1 непроходим целиком. В редакторе — сетка кликов
  во вкладке «Объекты» ниже инструментов и галка «на карте» (красный оверлей по всем
  расстановкам). Сетка хранится в файлах v2 (`items[i].pass` — строка "0101…",
  построчно сверху вниз) и едет вместе с картой. Для игр:
  `dual.buildCollisionMap({w, h, items, placements, ts})` →
  `{ w, h, blocked: Uint8Array, isBlocked(px, py) }` — `mapFromManifest/mapFromJSON`
  возвращают готовую `result.collision`. Кнопки «💾 все сетки» / «📂 сетки»
  экспортируют/применяют все сетки одним JSON-файлом (для ручной правки); файл,
  положенный как `images/objects/pass_defaults.json`, скрипт вырезки применяет
  к новым объектам поверх авторасчёта, а уже добавленные объекты хранят свои
  сетки в реестре как есть (append-only). Рытьё в земле («рытвины») по умолчанию
  непроходимо целиком.
- **Категории и «реальные размеры»**: у каждого объекта есть категория (`group`) —
  вкладка «Объекты» показывает кнопки-фильтры (Деревья / Растения / Вода / Камни /
  Рытвины / Руины / Строения / Мебель / Предметы / Магия / Снег), палитра фильтруется
  и сортируется по категории. Мелкие по смыслу объекты уменьшены до реальных размеров (ключи/свиток/
  книга — 32×32, а не габарит бочки) — настройка `SHRINK` скрипта вырезки
  (исторически в удалённом `cut_objects.py`; листу 7 не требовалась).

### Редактор карт (dualgrid_editor.html)

Полноценный редактор поверх модуля: те же слои, что и в рантайме (низ → верх),
каждый — свой тайлсет/частота/разброс/outside/hideBackground.

- **Инструменты:** ✏ кисть (размер 1×1…3×3) и 🧽 ластик красят **ячейки данных**;
  🏆 заливка (flood 4-связность); ⬚ выделение рамкой; 🏷 **штамп** — выбираешь тайл из
  палитры 4×4 активного тайлсета и кликом ставишь его (выставляются 4 ячейки под его
  углами; превью следует за курсором). Рисование ведётся по ячейкам данных, тайлы на
  границах пересчитываются автоматически (сущность Dual Grid).
- **Выделение (кусок карты, все слои):** Ctrl+C копия, Ctrl+X вырезать, Del стереть,
  Ctrl+V → клик = место вставки (с превью), Esc отмена.
- **Слои:** 👁 видимость, +/🗑/↑/↓; параметры активного слоя: частота/разброс/зазор
  слайдерами (применяются «🎲 Сгенерировать»), «скрывать фон», «вне карты» 0/1,
  залить/очистить/инверт. Зазор — минимум клеток травы до нижних слоёв с другим
  тайлсетом; нарушения (нарисовали воду вплотную к земле) подсвечиваются красным
  прямо в сетке данных (привязанный слой `on` из проверки исключён). Тайлсет,
  добавленный «📂 Тайлсеты…» поверх существующих
  слоёв, по умолчанию наслаивается («скрывать фон» включён) — иначе его
  непрозрачный фон накрыл бы карту; первый слой карты остаётся непрозрачным.
- **Генерация и файлы:** 🎲 генерирует ВСЕ слои по текущим параметрам (тот же код
  `mapFromManifest`, что и в движке) — размер берётся из полей ширины/высоты (при
  отличии от текущего сначала применяется resize); 🌍 «Сгенерировать мир» —
  биомные пояса + логичные объекты (см. «Генератор мира»); 💾 «Манифест» и 💾 «Карта (JSON)» —
  скачивание через браузер; 📂 открытие PNG-тайлсетов и JSON (манифест или карта — по
  полю `format`), работает и drag&drop; 🖼 экспорт карты в PNG.
- **Размер карты** 1..2048 (данные в пересечении сохраняются), «📄 Новая» чистит данные.
- **Отмена/повтор** (Ctrl+Z / Ctrl+Y, глубина 40) — полный снимок стека слоёв: краска,
  вставки, размер, генерация, добавление/удаление слоёв.
- **Навигация:** колесо — зум к курсору (2–800%), Space/ПКМ/СКМ + мышь — панорама,
  «⤢ вписать».
- **Вкладки «🟩 Земля / 🌳 Объекты»:** переключают левую панель. Во вкладке объектов —
  палитра 633 объектов (клик — выбор, призрак следует за курсором), ползунки «вес
  выбранного» (относительный шанс при генерации) и «плотность» (процент ячеек),
  галка «не ставить на воду» (при генерации и кистью — на воде кисть отказывает,
  призрак краснеет), кнопки «🎲 Объекты» (раскладать заново) и «🗑 убрать
  все». Кисть ставит выбранный объект (якорь — низ-центр ячейки), ластик стирает
  верхний объект под курсором; заливка/выделение/штамп — только во вкладке «Земля».
  «🎲 Сгенерировать» строит пол И объекты по манифесту.
- **Самопроверка** в статусе: каждый спрайт каждого слоя сверяется с `tileIndex()`,
  плюс корректность скрытия фона и каждый размещённый объект (позиция/якорь/текстура).
  API автоматики —
  `window.__MAPEDITOR` (рисование, буфер, save/load-объекты, undo/redo, selfTest,
  `obj`: place/erase/clear/generate/setWeight/setDensity/setAvoid/placements).

```js
// Рантайм: глобальная карта из манифеста (textures уже загружены игрой)
const { root, layers } = await dual.mapFromManifest(manifest, textures);
worldContainer.addChild(root);
// Локальный данж: готовая карта без генерации
const dungeon = await dual.mapFromJSON(JSON.parse(text), textures);
worldContainer.addChild(dungeon.root);
```

## Контроллер персонажа (modules/character.js, ECS-система)

Персонаж — обычная ECS-сущность, а не самостоятельный объект: позицию и спрайт
обслуживает ядро, контроллер добавляет только своё.

- **Ядро**: `positionX/positionY + spriteMap` → сущность попадает в `renderable`
  (renderSystem ставит спрайт на экран и кульит вне вида); `animationSpeed = fps/60`
  → в `animated` (animationSystem крутит кадры — спрайт создаётся с
  `autoUpdate=false`, `sprite.update(ticker)` зовёт именно ядро).
- **Свои компоненты** (через `ECS.registerComponent`): `ctrlSpeed/ctrlHalfW/ctrlHalfH/ctrlIdleT`
  (Float32), `ctrlFacing/ctrlMove/ctrlLock` (Uint8), `DATA.ctrlAnim` (текущая строка)
  и `DATA.ctrlAnims` (таблица строк сущности `{wait, walk_front, …}`).
- **Персонаж НЕ в `movable`**: ядро интегрирует скорость с отскоком от границ,
  а контроллеру нужно скольжение вдоль препятствий — система сама считает
  перемещение и пишет `positionX/positionY`.

### Фабрики

```js
const input = createCharacterInput({ target = window });   // WASD/стрелки + джойстик
const chars = createCharacterSystem({ world, ECS, COMPONENTS, DATA, blocked, input, addSystem? });
const id = chars.spawn({ x, y, sprite, anims, speed = 140, halfW = 7, halfH = 5, fps = 8 });
chars.update(ticker);      // каждый кадр игровой сцены (на паузе не зовётся — стоит)
chars.playAttack(id);      // attack_* в сторону взгляда (однократная)
chars.play(id, "death");   // любая однократная строка; по завершении авто-возврат
```

- `blocked(px, py)` — колбэк игры: «точка мира непроходима» (клетки объектов из
  `buildCollisionMap`, вода, край карты).
- `speed` — пикселей в СЕКУНДУ: система, как камера, живёт от `deltaMS` — скорость
  одинакова на 60 и 144 Гц.
- Ввод — общее устройство локальной игры (клавиатура + левый стик/крестовина,
  мёртвая зона 0.4); состояние персонажа живёт только в его компонентах.

### Поведение

- **Диагональ** = два соседних направления, скорость нормирована (не быстрее
  прямой). Диагональных анимаций нет — играет строка ПОСЛЕДНЕГО нажатого ещё
  удерживаемого направления (стек нажатий; взгляд сохраняется и в покое).
- **Скольжение вдоль препятствий**: оси двигаются НЕЗАВИСИМО — идёшь вправо,
  упёрся в стену, нажал вверх — едешь вдоль неё (и обогнёшь угол стены).
  Коллизия — AABB «ног» (halfW×halfH, по умолчанию 7×5 — меньше тайла 32px);
  по оси движения проверяется ТОЛЬКО переднее ребро (3 точки): остановка
  вплотную и чистое скольжение. Круг с диагональными пробами не годится:
  останавливает «не доезжая» до стены и навсегда цепляет угол клетки —
  персонаж застревал у карманов намертво.
- **Анимации**: walk_front/back/left/right — движение. Покой = СТОП-КАДР:
  персонаж замирает в кадре 0 walk-строки своего взгляда (`freezePose`,
  `gotoAndStop(0)`); спавн — `walk_front[0]`, взгляд вниз. После 4 секунд
  бездействия (`ctrlIdleT >= IDLE_WAIT_DELAY`) wait играется ОДИН раз
  (`play()`: `loop=false`, `ctrlLock=1`) и персонаж снова замирает в
  стоп-кадре; таймер идёт заново — при долгом покое wait повторяется каждые
  ~4.5 с. `play()` — однократные (attack_*, death, damage): `ctrlLock=1`,
  `sprite.onComplete` снимает блок; движение однократную не перебивает, но
  персонаж физически едет. ЛОВУШКА: `setAnim` с guard только по имени
  («строка та же — выходим») после freezePose замораживал ходьбу в ту же
  сторону — стоп-кадр оставляет ctrlAnim = walk_направление, но спрайт
  остановлен; guard обязан проверять и `sprite.playing`.
- **y-сортировка**: система пишет `sprite.zIndex = positionY`; в игре спрайт героя
  живёт в «ряду ног» контейнера объектов (см. следующий раздел) — сортируется
  только его ряд, герой честно заходит «за» стволы и кроны.

### В игре (scripts/game.js + index.html)

`index.html?w=500&h=500&seed=N` (по умолчанию 500×500, случайный сид) — при
старте генерируется открытый мир теми же функциями, что кнопка «🌍 Сгенерировать
мир» редактора (`dual.generateWorld` + `generateWorldObjects`, `noManMade: true`),
и спавнится оборотень `wolf_64` (кадры 64×64, 11 анимаций, 43 кадра, png +
JSON-манифест; получен из wolf_128 делением пополам — модальный цвет блока 2×2)
в ближайшей к центру свободной зоне 3×3. Коллизия «ног» — 7×5 px (halfW 3.5 ×
halfH 2.5 — вдвое меньше «стандартных» 14×10 под кадр 128). Рамки кадров
подрезаны на 0.05 px внутрь (PAD в game.js): UV ровно по границам кадров при
дробной фазе кромки спрайта на экране (нечётный размер канваса, нецелый зум)
захватывает крайнюю строку/столбец соседнего кадра атласа — тёмная линия стоп
«протекает» над головой при ходьбе влево/вправо. Камера следит за
компонентами сущности (getter-прокси `positionX/Y`). Управление: WASD/стрелки/джойстик — движение,
Space — атака, колесо — зум (0.5–4), средняя кнопка мыши — вернуть изначальный (×2.5),
P — пауза. Система персонажей вызывается из update
сцены game — на паузе замирает. Отладка: `window.__TEST` (`info()` — компоненты
сущности, `keyDown/keyUp` — синтетические клавиши для автотестов).

## Игровой мир: один слой пола и потоковые тайлы (modules/tilemap.js)

Четыре слоя пола (трава/вода/снег/песок) нужны только редактору — для рисования.
Игра несёт **один** слой и держит в живых **только видимые** тайлы.

- **Запекание при загрузке** (game.js): тайлы dualgrid полностью непрозрачны,
  поэтому для каждого угла двойной сетки достаточно верхнего непустого тайла
  (песок → снег → вода → база); всё, что под ним, отбрасывается. Результат —
  `cornerTex[(W+1)(H+1)]` ссылок на текстуры, на лету не пересчитывается.
  Ловушка: «чисто фоновый» тайл базовой раскладки — **12** (`lookup[0]`,
  `TILE_CORNERS[12] = [0,0,0,0]`), а не 0 — у наслагаемых слоёв он скрыт, как
  в `fill()`.
- **Потоковые тайлы** (`createTilemapSystem`): каждый тайл — полноценная
  ECS-сущность (`positionX/positionY + spriteMap` → `renderable`): спрайт ставит
  на место и кульит вне вида renderSystem ядра. Пул сущностей растёт лишь до
  размера окна камеры (+`margin` 2 тайла); когда камера уходит, сущность
  «паркуется» — снятие `positionX` выводит её из `renderable`, и ядро её не
  трогает (спрайт гаснет и остаётся своим: в пул ядра его нельзя — тот привязан
  к `configId` юнитов). Перестройка окна O(видимых тайлов) и только при
  пересечении камерой границы тайла или смене зума; вид берётся из трансформа
  контейнера мира — того же, что у culling ядра. Система создаётся ПОСЛЕ камеры,
  чтобы её тик шёл сразу за тиком камеры. `maxTiles` (60000) — предохранитель.
  ВАЖНО: координата, текстура и видимость пишутся на спрайт СРАЗУ при
  перестройке, не дожидаясь renderSystem: ядро наносит компоненты на спрайты ДО
  систем модулей, и без этого при движении на 1 кадр рисовалась «призрачная»
  вода/границы биомов на клетку позади камеры (вниз — выше места, вверх — ниже;
  на однородной траве сдвиг неотличим, поэтому рябило только у воды).
- **Объекты мира** (~23 тыс. на карте 500×500) — тоже ECS-сущности с culling
  ядра. Их спрайты разложены по **«рядам ног»** — полосам высотой TS: между
  рядами порядок задают сами контейнеры, сортировка по Y живёт только внутри
  ряда. Ряды статичны и не тасуются; единственное, что пересортировывает кадр, —
  ряд, куда пришёл герой (`sprite.zIndex = positionY` пишет система персонажей,
  между рядами героя переносит `wolfRowFollow` в game.js). Глобальная
  пересортировка 23 тыс. спрайтов вместо этого шла бы каждый кадр.
- **Зум** ограничен 0.5–4: площадь окна тайлов растёт как 1/zoom², при 0.5
  на экране 1280×720 их ~4.4 тыс. (сущностей всего ~28 тыс.) — 60+ FPS держится;
  сильнее отдалять не даёт колесо.

## Производительность (стенд: вкладка 143 Гц, мир 1600×1200, юниты с коллизиями и HP)

Замеры стресс-теста (полигон game.js, популяция наращивается через `window.__TEST.spawnWanderer`):

| Сущностей | До culling | Culling | + парные ячейки 32px + WebGPU | + точная ячейка 16px (итог) |
|---|---|---|---|---|
| 15 061 | 62 FPS / 17.4 мс | ~70 FPS / ~15 мс | 85 FPS / 12.6 мс* | ~100 FPS / 10.0 мс |
| 25 061 | 32.5 FPS / 31.4 мс | ~40 FPS | 43 FPS / 24.3 мс* | ~59 FPS / 16.9 мс |

\* на объектах до уполовнивания; после — 21.16 мс на 25k (та же конфигурация, свежий замер).

Порог «без лагов» (≥60 FPS): **~23 тыс.** объектов (21k → 67.7 FPS / 15.8 мс, 24k → 56.4 FPS / 18.4 мс).

Разложение выигрыша на 25 061 сущности (A/B, мс кадра): старая коллизия + WebGL — 31.44;
**парные ячейки + WebGL — 25.44 (−19% от алгоритма)**; парные ячейки + **WebGPU — 24.31
(−4% от рендерера)**. Вкладка теста поддерживает WebGPU — он реально активен
(`app.renderer.name === "webgpu"`); в браузерах без WebGPU движок сам откатывается на WebGL.

**Главный вывод:** после culling-а узкое место — не отрисовка и не плотность коллизий
(расширение мира вчетверо почти не помогало), а **линейные расходы на каждую сущность**:
движение, перестройка сетки, скан соседей коллизий, сам цикл рендера и обход сцены Pixi
(~0.6–0.7 мкс/сущность суммарно). Дальнейшие резервы (по убыванию эффекта):
LOD-пропускание физики для далёких/спящих групп, обход коллизий по парам ячеек вместо
скана 3×3 на каждую сущность, вынос физики в Web Worker (SharedArrayBuffer), WebGPU-рендерер.

**Culling в ядре (как работает):** `renderSystem` вычисляет вид-прямоугольник из трансформа
контейнера мира (`scale`, `pivot` + запас `CULL_MARGIN = 32`) и ставит спрайтам `visible`.
Без модуля камеры прямоугольник совпадает с экраном — поведение корректно само по себе.
`animationSystem` пропускает скрытых (видимость прошлого кадра, лаг 1 кадр перекрывается
запасом). Проверено: флаг видимости точно совпадает с прямоугольником вида на 23.5k.
Компонент `cullPad` (Float32) — запас для спрайтов, торчащих из точки позиции: у деревьев
и персонажей якорь — «ноги», крона выше точки на всю высоту текстуры, и точечный culling
появлял/прятал их разом целым куском. С `cullPad > 0` видимость считается по габариту
(`± pad`); объекты мира получают `max(высота, полуширина)`, герой — 128; 0 (по умолчанию) —
поведение прежнее.

**Игра на мире dualgrid (реальный кейс, экран 1280×720, сид 33981657):** до оптимизации
карта 500×500 несла 4 слоя по 250 000 спрайтов тайлов + 23 549 объектов = 1 023 549
спрайтов (349 028 рисовалось каждый кадр, объекты пересортировывались целиком) — **12 FPS**.
После запекания пола в один слой, потоковых тайлов и «рядов ног» объектов: 23 991
сущность ECS, в кадре 325–4 400 видимых спрайтов (зависит от зума) — **143–145 FPS**
(потолок монитора) в покое, при ходьбе и при зуме 0.5.

## Редактор персонажей (editor.html)

Пиксельный редактор: сетка 64×64 по умолчанию (**размер холста меняется 8..256**,
шаг 8, содержимое переносится с центровкой; зум подстраивается автоматически),
палитра + свой цвет, инструменты (пиксель / заливка / ластик / пипетка),
**строки-анимации** (именованные ряды: wait, death, …), кадры внутри строки,
проигрывание, экспорт сеткой: **строка = анимация, колонка = кадр**. Строки
выравниваются по максимальной длине (недостающие кадры — повтор последнего).
Результат: `images/sprites/<имя>.png` (сетка) + `<имя>.json` (манифест: size,
columns, **fps**, animations[{name,row,frames}]). Скорость проигрывания
(«кадр/с», поле у кнопки проигрывания) пишется в манифест: редактор играет с
ней, `assets.loadCharacter` отдаёт её как `fps` и `animationSpeed` (fps/60) —
достаточно `sprite.animationSpeed = char.animationSpeed`. Внизу редактора —
превью всего спрайтшита (клик по кадру — перейти к нему).
Два способа сохранения: **«Сохранить на сервер»** (POST /save — нужен сервер
разработки) и **«Скачать (download)»** (браузер скачивает PNG+JSON напрямую —
работает везде, включая file://).

**Открытие**: кнопка «Открыть» вызывает системный диалог выбора файла —
выберите .png и рядом его .json (файлы читаются локально, сервер не нужен).
Если .json не выбран, редактор пробует взять его из images/sprites/. Разбор
проверен round-trip: открыть → экспорт → открыть — пиксели совпадают.
Манифест обязателен: PNG без него не открывается.

Загрузка в игре — один вызов хелпера модуля assets (кэширует и PNG, и манифест;
на file:// — мимо fetch: манифест объектом, png data-URL):

```js
const assets = createAssets();
const mage = await assets.loadCharacter("images/sprites/mage_64");      // по http
const wolf = await assets.loadCharacter("wolf_64", EMBED.wolf);         // на file://
// mage.animations.wait / mage.animations.death — массивы текстур AnimatedSprite;
// переключение анимации: sprite.textures = mage.animations.death; sprite.gotoAndPlay(0);
// для проигрывания один раз: sprite.loop = false (смерть застывает на последнем кадре)
```

(формат манифеста: `{size, columns, animations:[{name,row,frames}]}` — при желании
доступен напрямую через `assets.get("<путь>#manifest")`).

**Готовые персонажи** (`images/sprites/` + код рисования в `scripts/drawings/`):
mage_64 (wait+death), elf_64 (4 walk), warrior_128, necromancer_128 и
barbarian_128 — стандартный набор (4 walk + 4 attack + wait + death + damage,
43 кадра). Рисователи с 128px используют кривые/эллипсы, автообводку силуэта,
наземную тень и форменные тени (свет сверху-слева); сборка кадров идемпотентна.
Правила слоёв эффектов: `flipX()` — последняя операция кадра (эффект отражается
вместе с персонажем); удар «по ту сторону» фигуры (вид сзади) рисуется ДО тела
(хук `behind`) — перекрывается силуэтом, поверх (`front`) только то, что в
воздухе. LN принимает только целые координаты (float → бесконечный цикл
Брезенхэма; в drawings LN округляет вход и имеет guard).

**priest_128** — персонаж, сгенерированный ИИ-пайплайном (SD-модель Retro Diffusion +
IPAdapter Plus + ControlNet openpose/depth; скелеты поз и карты глубины рендерятся
программно; пайплайн вне репозитория — D:\ZCode\ComfyUI). Стандарт 5×11, 43 кадра,
fps 8. Вид «сзади» генерится из отдельной back-базы (img2img d0.5, без лица),
«смерть»: ИИ даёт отшат/оседание, финальная «груда» — программное сплющивание
последнего кадра + затемнение. Лежачие позы модель не умеет — при замене на
процедурный рисовальщик учесть.

API автоматизации — `window.__EDITOR` (консоль/агент):

```js
E.px(x, y, "#ff0000");      E.rect(x, y, w, h, c);
E.mirror();                 // левая половина → правая
E.flipX();                  // полное зеркалирование кадра (вид слева ↔ справа)
E.resize(128); E.size();    // размер холста (8..256, содержимое центрируется)
E.addRow("death");          E.setRow("wait");   E.rows();
E.renameRow("wait", "walk_front");
E.frameData(row, i);        E.setFrameData(row, i, массив); // пиксели кадра
E.newFrame(); E.deleteFrame(i); E.setFrame(i); E.frameCount(); E.clear();
await E.open("mage_64.png");     // открыть PNG+манифест → кадры и анимации
await E.export("mage_64.png");   // mage_64.png + mage_64.json
```

Референсы: `elf_64.png` + `.json` (эльф-друид с посохом по референсу: 4 walk-анимации
по направлениям walk_front/back/left/right × 4 кадра — актуальный формат сетки 4×4),
`mage_64.png` + `mage_64.json` (маг: wait 4 + death 5, сетка 5×2),
`knight_64.png` (рыцарь, горизонтальная лента — старый формат).

## Стандартный набор анимаций персонажа

Конвенция игры (создаётся рисовальщиком, эталон — `scripts/drawings/warrior.js`,
ассет `images/sprites/warrior_128.png` + `.json`):

| Строки | Кадров | Описание |
|---|---|---|
| `walk_front/back/left/right` | 4 | ходьба в 4 направлениях |
| `attack_front/back/left/right` | 4 | замах → взмах → удар → возврат |
| `wait` | 4 | дыхание/покой — одиночное проигрывание после 4 с бездействия (в контроллере) |
| `death` | 5 | проигрывается ОДИН раз, `loop=false` (застывает на последнем кадре) |
| `damage` | 2 | получение урона без смерти (вспышка + отброс) |

Рисовальщик персонажа — обычный скрипт `function run(E) { … }` в
`scripts/drawings/<имя>.js` (примитивы редактора, палитра, параметрические части
тела и позы). Запуск: `new Function('E', src + ';return run;')(E)` после
`E.resize(128)`; весь рисунок внутри `E.silent(() => { … })` — примитивы не
перерисовывают доску на каждый вызов.

## Персонажи из forWork: пакер сырых полос (scripts/pack_characters.py)

Сырые заготовки лежат в `forWork/<enemy|hero>/<имя>/{move,attack,others}/*.png` —
по одной ГОРИЗОНТАЛЬНОЙ ПОЛОСЕ на анимацию, 4 кадра в полосе, спрайты стоят на
нижней кромке. `python scripts/pack_characters.py` склеивает их в листы 5×11
формата волка: `images/sprites/<имя>_<клетка>.png` + `.json` (fps 8). Размер
клетки: 64 (контент ≤64 — центр по X, низ с зазором 2px), 128 для кадров
128×128 (копия как есть). Папка `abil` у героев не входит (не стандартный набор).

- Контакт-листы всех персонажей (сетка, подписи, шахматный фон) пишутся в
  `tmp/forwork_preview/` — обязательная визуальная проверка каждого кадра.
- У rat нет `wait`, у spider/spiderRed нет `wait` и `damage`: wait синтезируется
  из кадра 0 `move/front` (1 кадр), damage просто отсутствует в манифесте
  (`play()` вернёт false). Строка в листе при этом остаётся пустой — формат 5×11
  у всех одинаковый.
- Пограничный контроль: полосы режутся по границам 4 колонок; колонки разрезов
  с плотной непрозрачностью печатаются предупреждением (возможный разрез по
  спрайту) — финальный арбитр всё равно контакт-лист.

**Листы персонажей** (23 врага + 4 героя) грузятся модулем assets
(`EMBED.chars` на file://). Витрина-строй NPC из тестов удалена из сцены
(мешала проверке мира и ИИ) — теперь на карте только герой и ИИ-враги
(см. «ИИ врагов»), а листы персонажей расходуются на боевые единицы.
Механизм рядов ног (`creatureRowFollow`) и `freeSpotNear` — общий.

## ИИ врагов (modules/ai.js)

Классическая схема RPG/MMO — FSM из трёх состояний над обычными сущностями
контроллера персонажей: **ПАТРУЛЬ** (случайные waypoint'ы в круге `patrolR`
вокруг точки спавна, паузы 1–3 с между ними) → герой вошёл в **aggro-радиус**
(`detectR`) → **ПОГОНЯ** (скорость ×1.25, в 36px враг останавливается и атакует
с перезарядкой 1.4 с) → герой пропал из радиуса ×1.6 или враг вытянулся за
**поводок** `leashR` от дома → **ВОЗВРАТ** (героя не замечает до прихода —
классический reset) → снова патруль. Источник паттерна — общепринятая практика
(gamedev.stackexchange: «aggro radius + return home»; FSM-туториалы
patrol→chase→attack→return).

- **Виртуальный ввод**: ИИ не двигает сущности сам — пишет `ctrlVI/ctrlVJ`
  (оси −1..1), а движение с коллизиями/скольжением, анимации ходьбы и модель
  покоя делает контроллер персонажей. Бит `ctrlVI` отличает ИИ-сущность: она не
  слышит общие клавиши. Взгляд в движении — доминирующая ось вектора.
- **Битовый бюджет**: маска — Uint32 (32 компонента). В `ai.js` регистрируются
  только 3 обязательных компонента (`aiState`, `aiHomeX/Y` — хот-данные);
  радиусы, таймеры, waypoint и перезарядка лежат в обычном массиве `STATE`
  фабрики (один object-lookup на врага в кадр, без бита).
- **Пловцы**: компонент `ctrlSwim` + второй предикат проходимости `blockedAlt`
  (у контроллера И у ИИ): вода проходима, суша — стена. Октопусы патрулируют
  озеро и «прессуются» к берегу под героем, не выходя из воды.
- **Расселение** (game.js): 12 деревень (гоблины + шаман-элитка, дворфы,
  орки + огр-элитка; элитка в центре, 4–6 сородичей кольцом, сайты не ближе
  45 клеток друг к другу и 20 от спавна героя), 40 лесных (пауки/крысы —
  проходимые клетки под кронами группы `trees` реестра) и 14 октопусов
  (внутренние клетки озёр 3×3 воды). Итого ~120 ИИ-сущностей, FPS не проседает.
- **ЛОВУШКА (выловлена тестом)**: у закуленной сущности animationSystem ядра
  не крутит кадры → `onComplete` однократной анимации не наступает и
  `ctrlLock` зависает навсегда (персонаж навечно «застывает» в wait/attack).
  Поэтому `play()` не запускает однократную у невидимых спрайтов — таймер
  покоя запустит её, когда сущность появится на экране.
- В `__TEST`: `enemies` (имя/id/размер), `ai.STATE` (состояния по id),
  `ai.STATE_NAMES`.

## Снаряды (modules/projectiles.js + data/attacks.js)

// АТАКИ И СНАРЯДЫ — привязка «персонаж → атака → снаряд» (data/attacks.js).
// ATTACK_CONFIGS — типы атак: kind — «melee» (эффект ПЕРЕД персонажем, не
// двигается, анимация однократно, время жизни = длина анимации) или «ranged»
// (снаряд вылетает и летит, гаснет по lifetime из атаки персонажа); reach —
// «зона потенциальной достижимости оружия» (px): автоатака срабатывает, когда
// цель вошла в радиус (у ИИ — дистанция остановки для стрельбы).

- **Листы снарядов** собирает `scripts/pack_projectiles.py` из
  `forWork/attacks/<атака>/*.png`: `all.png` — универсальная анимация,
  `front/back/left/right.png` (+ диагонали у knife) — своего направления;
  ОДИН лист `<атака>.png` + `.json` на атаку, строка = направление, формат
  манифеста как у персонажей (грузится тем же `assets.loadCharacter`). Кадры
  сегментируются по прозрачным колонкам (ширина кадров внутри атаки гуляет);
  слипшиеся полосы чинятся переопределениями FRAME_COUNT_OVERRIDES («разделить
  на N равных»). Контакт-листы → `tmp/forwork_preview/projectiles/`.
- **Выпуск**: персонаж с привязкой (`projectiles.bind(id, вид)`) играет
  attack_* → на кадре `spawnTick` рождается эффект/снаряд: melee — из «груди»
  чуть впереди, стоит на месте (`vx/vy = 0`), `loop=false` — анимация
  проигрывается ОДИН раз и эффект гаснет (lifetime = кадры/fps); ranged —
  летит прямолинейно (`vx/vy` по направлению × speed), кадры крутит ядро
  (loop), по истечении `lifetime` гасится в пул своего типа. Сущность несёт
  только существующие компоненты (positionX/Y + spriteMap + animationSpeed +
  cullPad) — ноль новых бит; снаряды ходят по «рядам ног» и честно заходят
  за стволы.
- **Автоатака героя**: в сцене game герой сам бьёт ближайшего врага, вошедшего
  в reach его оружия (кулдаун 0.9 с, рефлексы сокращают); клавиши атаки нет.
  ИИ-враги атакуют так же: дальнобойные останавливаются на дистанции reach
  и стреляют, ближние подходят на 0.6·reach. Коллизии/урон — модуль combat
  (см. «Боевая и ролевая система»).
- `file://`: листы вшиты в `EMBED.proj` (пересборщик `make_embedded_assets.py`
  читает и `images/projectiles/`).
- В `__TEST`: `projectiles` (`TYPES/BOUND/PROJ/ACTIVE` — активные снаряды).

## Боевая и ролевая система (modules/combat.js + data/stats.js)

// Ролевая модель — формулы из образца forWork/countDopStats.js (без реликвий
// и инвентаря): 5 основных характеристик (Сила/Ловкость/Здоровье/Скорость/
// Мудрость) → по 3 вторичных стата от каждой. countLog(i) = trunc(((1+40/i)^
// (i/40) − 1)/(e−1)·100) — универсальный переводчик стата в процент: блок =
// countLog(Сила/2), крит = countLog(Ловк), мощь крита = 150 + 2·countLog(Ловк)
// (начинается со 150%), уклонение = countLog(Ловк/2), ХП = 10 + 5·Здоровье,
// подвижность/рефлексы и т.д. Опыт до уровня — кривая пользователя:
// xpToNext(lvl) = trunc(((1+10/lvl)^(lvl/10) − 1)/(e−1)·100) (15 → 100).

- **Битовый бюджет: ноль новых компонентов** (занято 30/32). ХП живут в
  компонентах `hp/maxHp` — их при загрузке страницы регистрирует modules/health.js
  (стиль того модуля устарел, но регистрация идемпотентна и переиспользуется).
  Ролевые данные (STAT/FACTION) — обычные массивы фабрики, как STATE в ai.js.
- **Разрешение удара** (`dealDamage`): уклонение (весь урон мимо, «уклон») →
  крит бьющего (урон × мощь крита, большая жёлтая цифра «N!») → блок
  (половина урона, серая цифра) → каменная кожа цели (`special.stoneskin` —
  кап урона за удар) → вычет ХП. После урона: наложение яда бьющего
  (`special.poison`) и событие `onDamaged` (зов соратников). Каждое событие —
  всплывающая цифра над головой (modules/fx.js, слой над объектами) + красная
  вспышка спрайта. Урон бросается при ВЫЛЕТЕ снаряда: `rollAttack(id)` →
  weaponMin..attack (attack = Сила), снимок едет на снаряде (`PROJ[].attack`),
  поэтому смерть стрелка в полёте не ломает попадание.
- **Особые способности врагов** (`special` в ENEMY_STATS, справочник —
  ENEMY_BESTIARY data/enemies.js). Активны: `stoneskin` (орк — кап 12/удар),
  `poison` (паук — тление 1 урона/с, зелёные цифры, может убить; убийство
  засчитывается отравившему), `call` (гоблин — колбэк `onDamaged` в game.js →
  `ai.alert(x, y, R)` поднимает соратников в радиусе из патруля в погоню).
  Остальные ключи бестиария (rage/reanimate/flame/shadow/dash/charm/howl/
  summoning/vampirism/crush/voidBlob/segmentation/invulnerability) — контент
  под данжи, механики появятся вместе со спавном этих видов.
- **Попадания**: melee-эффект бьёт разовым АоЕ (радиус 40) в момент появления;
  летящий снаряд бьёт первую противника под собой (радиус = size·0.3+4) и гаснет.
  Фракции: 0 — сторона героя, 1 — враги (снаряд бьёт только чужую фракцию).
- **Полоска ХП** — ребёнок спрайта (culling и «ряды ног» достаются бесплатно),
  показывается только когда ХП не полные. Контратака: выжив под melee-ударом —
  шанс ответить своим оружием (гвард 1.2 с против пинг-понга).
- **Смерть**: анимация death держит последний кадр (свой onComplete — штатный
  снял бы ctrlLock, и «труп встал»), корпус тает и убирается (спрайт мимо
  ядерного пула, `BOUND[id]=null`, колбэк `onRemove` чистит ряды ног и хендлы
  сцены). Герой вместо уборки возрождается через 2.5 с с полным ХП.
- **Опыт и уровни** (только герою): награда за вид (ENEMY_STATS[kind].xp),
  обучаемость может удвоить; на уровне растут основные статы (HERO_BASE.growth
  +2 всё), полный хил, пересчёт скорости (подвижность). Статы врагов —
  ENEMY_STATS (уровень/статы/weaponMin/награда) + разброска +0..1 при спавне.
- **Заделы на будущее** (считаются, но не применяются — способностей/дебафов
  ещё нет): сопротивление, выносливость (+% к лечению — применяется в heal),
  находчивость, сила воли, контрмагия. Применяются сейчас: подвижность
  (скорость), рефлексы (кулдаун автоатаки героя), обучаемость.
- **HUD героя** (правый верх): «Волк · ур. N · HP/Max» + полоски ХП и опыта
  (modules/hud.bar). Отладка: `debug.info["ХП"]`, в `__TEST`: `combat`,
  `heroStat()`, `heroDop()`, `giveXp(n)`.
- **Прицельность и классы дистанционных атак** (слова пользователя): летящий
  снаряд с 4 анимациями (front/back/left/right) летит СТРОГО по вертикали или
  горизонтали взгляда атакующего; под произвольным углом (например по
  диагонали, в цель) летят только снаряды с ОДНОЙ анимацией `all` — лист
  классифицируется при загрузке (`TYPES[].freeAngle`). Прицел `playAttack(id, aim)`
  применяют только all-снаряды (шаман staff, паукоид web, октопус octo,
  огр club и т.д.),
  осевые (bow, poison) его игнорируют — на диагоналях они целенаправленно
  промахиваются, это честное поведение класса. Привязки видов и kind атак
  выверены по образцу (см. раздел «Контент из образца»): гоблин и нетопырь —
  melee-укол кинжалом, орк — булава, огр — БРОСОК дубины (ranged, all-лист,
  свободный угол), шип — каменный удар вблизи, тёмный воин — копьё-снаряд.
- **Игровой шрифт** всех надписей (HUD, отладка, цифры урона) —
  `MixSerifCondenseRUS` из `forWork/*.ttf`, вшит base64 в `scripts/game_font.js`
  (генератор `scripts/make_game_font.py`; на file:// внешний ttf — чужой origin,
  data-URL работает всюду). game.js зовёт `await loadGameFont()` до первых
  PIXI.Text; дефолт fontFamily в modules/hud.js, fx.js, debug.js — `GAME_FONT`.
  Размеры с 2026-09-15 удвоены по требованию пользователя (24–28 базово,
  цифры урона 22/28, пауза 64); ширина полос героя 220/176 px.
- **UI-спрайты** (`images/ui`, исходники в `forWork/UI`): курсор игры —
  `cur.png` через CSS `body { cursor: url(...) 0 0, auto }` (остриё в пикселе
  0,0); рамка загрузки `bigBar.png` (407×64) + линия `loadBarLine.png` (315×24,
  в рамке на 46,20) — экран загрузки по центру на старте: `setStage(метка,
  доля 0..1)` тикает по этапам бута (тайлсеты → реестр → генерация → запекание
  → объекты → персонаж → враги), при входе в игру бар уничтожается.
  Полоски HUD героя — тоже спрайты (фабрика `makeSpriteBar` в game.js):
  ХП — `bigBar` + красная `hpBarLine` (220px, scale 0.54), опыт — `smallBar` +
  золотая линия (176px = на 20% короче, scale 0.8), центры полос — на общей
  оси симметрии. Полоски привязаны к ПРАВОМУ КРАЮ экрана: позиции
  пересчитываются в обработчике `app.renderer.on("resize")` — при изменении
  окна/фуллскрине остаются в углу (y: ХП 12, опыт 51; текст над полосками
  убран — уровень/ХП видны в оверлее отладки).
  ⚠️ ЛОВУШКА v8: заливку полос НЕ делать через Graphics-маску
  (`renderable=false` не отсекает линию) и НЕ через мутацию `texture.frame`
  (UV минифицированного билда не обновляются — линия всегда «полная»,
  вылезает за рамку). Рабочий способ — `PIXI.NineSliceSprite`
  ({left:14, right:14, top:10, bottom:10}): `width` = доля заполнения,
  середина растягивается, торцы не искажаются; у текстур PIXI.Assets нет
  `.clone()`. Красная `hpBarLine.png` — та же заливка в ХП-полосе; остальные
  спрайты папки (`hpBarLine` уже подключён, другие — под будущие полоски) на
  file:// вшиты в `EMBED.ui` (make_embedded_assets.py собирает images/ui).
- **Отладка без отрисовки**: `index.html?raf=timeout` подменяет
  requestAnimationFrame на setTimeout (до подключения Pixi) — игра грузится и
  тикает на скрытой/перекрытой панели (автотесты); на обычном запуске не
  включается.
- **Стартовое меню** (сцена "menu"): после загрузки мира игра открывает меню —
  фон `start.png` (cover на весь экран), колонка кнопок `button.png`
  (NineSlice, 280×60, слева на лесном краю арта): Продолжить / Новая игра
  (перезагрузка со случайным сидом) / Настройки / Выход (window.close, иначе
  экран завершения). «Настройки» — панель с переключателем полного экрана,
  Escape закрывает. Указатель на кнопках — cursor pointer, hover — подсветка.
  Сцена "game" показывает подсказку управления, "menu" прячет. Кнопкам меню
  нужен `app.stage.eventMode = "static"`.

## Квесты (modules/quests.js + data/quests.js)

Цепочка «Путь вожака стаи» — 10 последовательных заданий, данные отдельно от
логики (`data/quests.js`: `QUEST_CHAIN` + `KIND_RU`). Активным всегда ровно
одно задание; следующее открывается после выполнения предыдущего. NPC нет —
модуль сам объявляет задания. Состояние сессионное (сохранений нет).

Типы целей: `kind: "rat"|…` — убить N врагов вида (ключ `ENEMY_STATS`),
`kind: "any"` — N любых врагов, `level: N` — достигнуть уровня N. Награда
(`reward`) — опыт через `combat.giveXp`.

- **События**: убийства героем приходят от боя — `combat.js` зовёт колбэк
  `onKill(killerId, victimId)` в конце `awardKill` (только реальные награды за
  убийство; вид жертвы game.js берёт из массива `enemies` — жертва покидает
  его позже, при уборке трупа). Задания «достичь уровня» модуль опрашивает сам
  каждый кадр (`combat.stat(heroId).lvl`). Тик — ТОЛЬКО в игровой сцене
  (`quests.update(ticker)` в scenes "game"), не через addSystem: объявления не
  тикают в меню/паузе.
- **Интерфейс** — свой контейнер в app.stage (`show(false)` в сцене menu):
  трекер активного задания под полосками ХП/опыта справа (300px, правый край
  при resize через `place()`); журнал всей цепочки по клавише **L** (Escape
  закрывает; маркеры: зелёный — выполнено, жёлтый — активно, контур — закрыто;
  внизу — описание активного); объявления — очередь плашек сверху экрана
  (2.8 с, затухание). Прогресс убийств дублируется всплывашкой «3/5» над
  героем (fx). Первое задание объявляется при входе в игру.
- **Награды и баланс**: награда ≈ половина опыта за целевые убийства; цепочка:
  крысы ×5 → уровень 4 → гоблины ×6 → пауки ×4 → дворфы ×4 → шаманы ×2 →
  орки ×5 → любые ×10 → огры ×2 → любые ×20 (итого ~485 опыта наградами).

## Контент из образца (forWork/example): герои, бестиарий, умения

Образец — рогалик с метой (SVG-движок, медленный); его КОНТЕНТ переносится
на наши системы (PixiJS+ECS). Источник: `example/build/scripts/data.js`
(герои/враги/атаки/умения) + `localization.js` (RU-тексты). Тик образца —
16 мс (62.5 Гц): все cooldown/duration переведены в секунды (÷62.5).

- **data/heroes.js** — 4 класса образца (Плут/Волшебница/Рыцарь/Валькирия):
  20 очков по 5 статам, оружие, дерево умений (`tree` → ABILITY_TREES).
  Ключи = ключам листов и CHARACTER_ATTACKS (rogue/sorca/knight/valca, листы
  уже в images/sprites). В мире пока не спавнятся — задел под выбор класса;
  действующий герой — волк (HERO_BASE в data/stats.js, другой масштаб: 37 очков).
- **data/abilities.js** — 4 дерева × 14 умений (56 узлов): RU-названия и
  описания ПО УРОВНЯМ (desc.length = maxLvl), cooldown/duration в секундах,
  damage — базовый урон активного эффекта, граф prev/next, сетка x/y,
  иконки `images/abilities/<класс>/<n>.png` (128×128, скопированы из образца).
  Деревья — КОНТЕНТ под будущий экран класса (система прокачки не построена);
  БОЕВОЙ набор героя живёт там же — WOLF_ABILITIES (см. «Способности героя»).
- **data/enemies.js** (ENEMY_BESTIARY) — полный бестиарий образца: 23 вида
  (15 рядовых / 6 элита / 5 боссов, этажи 1–4 образца), сырые параметры
  (hp/dmg/exp/speed/rage — масштаб образца, справочно), особая способность
  `special` с описанием, привязка к нашей атаке и ключу спрайта. Наши боевые
  статы спавнящихся видов — по-прежнему ENEMY_STATS (data/stats.js); там же
  их `special`. Консистентность бестиария/атак/спрайтов/иконок проверяется
  скриптом (все 23 вида имеют листы и записи в CHARACTER_ATTACKS).
- **Атаки по образцу** (data/attacks.js): `cd` — перезарядка атаки в секундах
  (темп атак ИИ: ai.register attackCd); kind и привязки выверены: dagger —
  melee-укол (гоблин, нетопырь), club — бросок дубины огра (ranged, all-лист,
  свободный угол), rock — каменный удар шипа (melee), орк — булава, лидер —
  алебарда, мумия — двуручный меч, тёмный воин — копьё, вампир/пауки — укус,
  имп — алебарда, босс-паук — плевок (poison), демон — молния (boss3attack),
  циклоп/медуза — звезда пустоты (void).
- **Активные механики особых способностей** (modules/combat.js, см. раздел
  «Боевая система»): stoneskin / poison / call. Остальные — фаза данжей.

## Способности героя (modules/abilities.js + WOLF_ABILITIES)

Боевой набор волка — 3 активных умения (адаптации умений образца), панель
слотов внизу по центру с клавишами **1–3** (подсказка управления дополнена).

- **Данные** (`WOLF_ABILITIES` в data/abilities.js): `{ key, name, icon,
  cooldown, params, desc }` — параметры отдельно, механика — в модуле (RUN по
  ключу). Огненный шар (снаряд в ближайшего врага, урон 6 + 1×мудрость, кд 5с),
  Пронзающий рывок (шагами по взгляду до 96px с гашением о стены, задетые
  получают 2 + подвижность/2, кд 6с), Мороз (волна в радиусе 150: 4 + мудрость/2
  и замедление ×0.5 на 2.5с, кд 12с).
- **Механики**: снаряд шара выпусруется прямым `projectiles.spawn` с СНИМКОМ
  урона владельца (`attack: { base, critPower }`) — тот же путь, что у
  автоатаки: SPELL может уклониться/крит/блок и убить. «Нет цели» — каст не
  тратит кулдаун. Рывок двигает героя шагами 4px с проверкой `blocked`.
  Замедление — `combat.applySlow(id, mul, dur)` (STAT.slowT/slowMul, скорость
  пересчитывается из базовой; по истечении восстанавливается; на время
  замедления буст погони ИИ ×1.25 пропадает — приближение).
- **UI**: слот 52px — Graphics-рамка (fill 0x140f08/stroke 0x6b4a2f, готовый —
  stroke 0xffd98e), иконка Sprite, тёмная шторка кулдауна сверху (высота =
  доля остатка) + секунды по центру (целые, ниже 1с — десятые), цифра клавиши
  в углу. `place()` на resize; `show(false)` в меню, `show(true)` в игре.
- **ТИК ТОЛЬКО В ИГРОВОЙ СЦЕНЕ** (как квесты): `abilities.update(ticker)` из
  scene update — на паузе кулдауны замораживаются; клавиши читаются там же.
- **Иконки на file://**: генератор `scripts/make_embedded_abilities.py`
  вшивает ТОЛЬКО иконки боевого набора в `scripts/embedded_abilities.js`
  (глобаль EMBED_ABILITIES, ~121 КБ на 3 иконки) — новое умение героя:
  добавить его icon в список генератора и перезапустить. По http — живые
  файлы images/abilities.
- Ноль новых компонентов: кулдауны — объекты слотов фабрики. `__TEST`:
  `abilities`, `abilitiesState()` (снапшот кд).

## Отладка

Глобальные хендлы (после `init()`):

```js
window.__ENGINE  // { app, ECS, world, COMPONENTS, DATA, SpatialHashGrid,
                 //   particleContainer, worldContainer, STATIC/ANIMATED_SPRITE_POOLS, events }
window.__TEST    // полигон game.js: счёт, health, camera, scheduler, fx, audio, input, scenes
```

Горячие клавиши: **F3** — оверлей отладки, **G** — сетка коллизий, **H** — хитбоксы (рисуются
в мировых координатах, двигаются с камерой).

Тестовый полигон `game.js`: мир 1600×1200 (больше экрана), игрок (синий, WASD/стрелки),
60 блуждающих юнитов с HP. Таймер бьёт случайного юнита на 35 каждые 2 с (смерть → взрыв
частиц + звук + счёт), каждые 3 с популяция восполняется, игрок регенерирует 5 HP/с.
**P** — пауза (таймеры замирают), **K** — урон игроку, колесо — зум, ЛКМ — всплеск частиц
(и разблокировка звука).

## История изменений (кратко)

- **2026-09-10** — рефакторинг ядра (Map-баг коллизий, `PIXI.AnimatedSprite`, разделённые
  пулы, swap-and-pop); модульное расширение (11 опциональных модулей, `addSystem`); culling;
  парные коллизии + WebGPU; двухуровневая сетка коллизий — отрицательный результат,
  выключена (`useCoarse: false`). Подробности и цифры — в «Производительности».
- **2026-09-12** — модуль `dualgrid.js` (Dual Grid System, `generateMap` с точной частотой,
  форматы манифест/карта v1, `detectLayout`); редактор `dualgrid_editor.html` (кисть/штамп/
  выделение/undo, file://-режим с вшитыми data-URL тайлсетов); зазор `margin` между
  местностями с разными тайлсетом.
- **2026-09-13** — слой объектов: листы 1–6 (405 объектов, категории, SHINK реальных
  размеров, дедуп по сигнатуре 48×48, проходимость `pass` + `buildCollisionMap/isBlocked`,
  «не ставить на воду»); лист 7 «рытвины и скелеты» (+29 → 434, группа `pits`); append-only
  пайплайн: `cut_objects.py` удалён, новые листы дописывают `cut_sheetN.py`.
- **2026-09-13/14** — тайлсеты: привязка слоя к фичам нижнего (`on` + `erodeMask`),
  `snow_dirt`/`sand_dirt` — дефолтные наслаиваемые слои (margin 2, on=grass_dirt), новые
  слои из «📂 Тайлсеты…» по умолчанию наслаиваются, красная подсветка игнорирует
  привязанный слой; размер карты 1..2048, «🎲 Сгенерировать» применяет размер из полей.
- **2026-09-14** — лист 8 «снежные объекты»: +160 (→ 594, группа `snow`, категория «Снег»);
  `cut_sheet8.py` — кремовый фон (дистанционный флуд + un-blend от цвета фона), разрезка
  слипшихся тенями рисунков по тёмным контурам с эрозией мостиков, имена частей в
  `cut_sheet8_names.json`.
- **2026-09-14** — генератор мира «🌍 Сгенерировать мир»: `generateWorld` (биомные пояса
  по температуре: север — снега, юг — пустыня с оазисами; поля вероятностей +
  `rankNormalize`), `generateWorldObjects` (POI-кластеры деревень/руин/костищ +
  амбиент по биомам), кнопка в редакторе; зум редактора до 2% (вписать 500×500).
- **2026-09-14** — лист 9 «водная поверхность»: +39 водных декалей (группа `water`,
  категория «Вода»; реестр 633) — `cut_sheet9.py` берёт с листа только водные объекты,
  повторки пропускает; в генераторе мира проход 3 раскладывает их ~2% водных клеток.
- **2026-09-14** — естественная растительность: лесные массивы-стенды `WORLD_VEG`
  (доминант+спутники+подлесок по биомам: дубравы/берёзняки/ельники/ксерофиты),
  двухпроходная посадка (полог → подрост), градиент чаща/опушка/луг, поляны,
  ивы и камыш у берегов; садово-парковые растения в амбиент не входят.
- **2026-09-14** — игра 60+ FPS на мире 500×500 (было 12): 4 слоя пола запекаются
  в один при загрузке (перекрытые тайлы отбрасываются; ловушка — фоновый тайл
  раскладки `lookup[0]` = 12, не 0); модуль `tilemap.js` держит только видимые
  тайлы — пул ECS-сущностей, парковка снятием `positionX`; объекты мира —
  ECS-сущности, спрайты по «рядам ног» (сортировка по Y только внутри ряда,
  герой переезжает между рядами); зум ограничен 0.5–4. Замеры — в
  «Производительности».
- **2026-09-14** — персонажи 64×64 вместо 128×128: `wolf_64.png`/`.json`
  (спрайтшит и манифест `size` пополам — модальный цвет блока 2×2, палитра и
  обводка не страдают), коллизия «ног» 7×5, изначальный зум поднят 2 → 2.5;
  wolf_128 остался как исходник.
- **2026-09-14** — правки игры: «призрачная вода» при движении устранена —
  позиция/видимость тайла пишутся на спрайт сразу при перестройке окна (ядро
  наносит компоненты до систем модулей, лаг в 1 кадр давал мелькание на клетку
  позади движения); деревья выплывают из-за края экрана постепенно — компонент
  `cullPad` в ядре (culling по габариту спрайта, а не по точке); средняя кнопка
  мыши возвращает изначальный зум.
- **2026-09-14** — контроллер персонажа: `modules/character.js` — ECS-система
  (персонаж = сущность positionX/Y+spriteMap+animationSpeed + ctrl-компоненты;
  ядро ставит спрайт и крутит кадры). Движение со скольжением вдоль препятствий
  (AABB «ног», проверка переднего ребра по оси), диагональ = анимация последнего
  нажатого направления, клавиатура + джойстик. `game.js` стал настоящей игрой:
  генерация мира через функции редактора + оборотень `wolf_128` (старый полигон
  с шариками удалён).
- **2026-09-14** — игра работает двойным кликом: весь движок переведён в dual-mode
  (глобали без `import/export`, порядок `<script>`-тегов в `index.html`); для
  `file://` ассеты игры вшиты в `scripts/embedded_assets.js` (генерирует
  `make_embedded_assets.py`: тайлсеты PNG + оборотень WebP-lossless data-URL —
  картинки с диска на `file://` чужой origin для GPU). `game.js` — классический
  скрипт (IIFE).
- **2026-09-14** — `images/rebuild_registry.py` стал ЕДИНЫМ пересборщиком
  запечённых ассетов: помимо реестра объектов обновляет `scripts/embedded_assets.js`
  (игра на file://) и вшитые тайлсеты редактора (`EMBEDDED_TILESETS` в
  `dualgrid_editor.html`) — только реально изменившиеся места. Повод: правка
  волка не попадала в игру — пересборщик трогал только реестр объектов, а волк
  на file:// живёт в embedded_assets.js. `nocache-server.py` принимает порт
  аргументом (`python nocache-server.py 8791`).
- **2026-09-14** — «чёрная полоска над головой» при ходьбе влево/вправо
  (воспроизводилась только в окне с нечётной шириной/высотой и на максимальном
  зуме): UV кадров атласа были ровно по границам, и при дробной фазе кромки
  спрайта на экране GPU захватывал нижнюю тёмную строку стоп кадра строкой
  выше. Фикс — подрезка рамок кадров на 0.05 px внутрь (PAD). На полный экран
  не воспроизводилось: чётные размеры канваса держат кромки на целых пикселях.
- **2026-09-14** — 27 персонажей из `forWork` (23 врага + 4 героя): пакер
  `scripts/pack_characters.py` склеивает сырые полосы (4 кадра, 32/48/64/128px)
  в листы 5×11 формата волка; вся игра переведена на модуль `assets` (PAD теперь
  в его нарезке, `loadCharacter` принимает вшитый режим — единый путь нарезки);
  `make_embedded_assets.py` вшивает всех персонажей (`EMBED.chars`); NPC строем
  на тестовой карте ходят/атакуют вместе с героем (ввод общий). Подробности —
  «Персонажи из forWork».
- **2026-09-14** — новая модель покоя контроллера персонажей: спавн — стоп-кадр
  `walk_front[0]` (взгляд вниз); в покое персонаж замерает в кадре 0 walk-строки
  своего взгляда (вместо зацикленного wait); после 4 с бездействия wait
  проигрывается ОДИН раз и персонаж возвращается в стоп-кадр (компонент
  `ctrlIdleT`).
- **2026-09-14** — ИИ врагов: модуль `ai.js` (FSM патруль → погоня → возврат,
  aggro-радиус/поводок/waypoints) + виртуальный ввод в контроллере
  (`ctrlVI/ctrlVJ` — ИИ-сущности не слышат общие клавиши) + пловцы
  (`ctrlSwim` + `blockedAlt`). На тестовой карте: 12 деревень с элитками,
  лесные пауки/крысы, октопусы в озёрах — ~120 сущностей. Ловушка: у закуленных
  сущностей однократные анимации не запускаются (кадры не крутятся —
  `ctrlLock` завис бы навсегда). Подробности — «ИИ врагов».
- **2026-09-14** — снаряды: `pack_projectiles.py` собирает 22 типа из
  `forWork/attacks` (лист на атаку, строка = направление; сегментация по
  прозрачным колонкам + переопределения для слипшихся кадров); `data/attacks.js`
  связывает 28 персонажей с типами снарядов (spawnTick, lifetime); модуль
  `projectiles.js` выпускает снаряд на кадре атаки, летит прямолинейно, гаснет
  по времени жизни, пул на тип, ходит по «рядам ног», ноль новых бит.
  Подробности — «Снаряды».
- **2026-09-14** — витрина-строй NPC (27 персонажей у спавна, ходили за
  игроком) удалена из сцены: на карте герой + ИИ-враги. Листы персонажей
  остаются в `images/sprites` (расходуются врагами и будущими боевыми единицами).
- **2026-09-14** — атаки разделены на классы и стали автоматическими:
  `kind: melee` (эффект перед персонажем, без движения, однократно) /
  `ranged` (снаряд летит); `reach` — зона достижимости оружия. Атака героя
  убрана с пробела: автоатака ближайшего врага в reach (кулдаун 0.9 с);
  ИИ-враги останавливаются на дистанции `attackR` своего оружия.
- **2026-09-15** — боевая и ролевая система: `data/stats.js` (формулы образца
  `countDopStats.js`: countLog → проценты, кривая опыта пользователя, статы
  героя и 8 видов врагов) + `modules/combat.js` (ХП в готовых компонентах
  `hp/maxHp` — ноль новых бит; уклон → крит → блок; контратака; смерть с
  тающим трупом и возрождением героя; опыт/уровни; полоски ХП над головами;
  всплывающие цифры через `fx`). Снаряды несут снимок боевых данных владельца
  (фракция + бросок урона) — попадания решает combat. Прицельная стрельба:
  `playAttack(id, aim)`, melee-ИИ подходит на 0.6·reach. Подробности —
  «Боевая и ролевая система».
- **2026-09-15** — отладочный режим `?raf=timeout`: игра грузится и тикает без
  реальных кадров (RAF на setTimeout) — для автотестов на скрытой панели;
  без параметра ничего не меняется.
- **2026-09-15** — классы дистанционных атак (слова пользователя): снаряды с
  4 анимациями летят только по вертикали/горизонтали взгляда; произвольный
  угол — только у снарядов с одной анимацией all (`TYPES[].freeAngle`).
  Игровой шрифт всех надписей `MixSerifCondenseRUS` вшит в `game_font.js`
  (генератор `make_game_font.py`), дефолт hud/fx/debug.
- **2026-09-15** — шрифт укрупнён ×2 (слова пользователя: «слишком мелкий и
  нечитаемый»); курсор игры `images/ui/cur.png` (CSS, остриё 0,0); экран
  загрузки на спрайтах `bigBar` + `loadBarLine` по центру черного экрана
  (прогресс по этапам бута через `setStage(метка, доля)`); UI-спрайты
  `images/ui` вшиваются в `EMBED.ui`.
- **2026-09-15** — полоски ХП/опыта героя на спрайтах (`bigBar`/`smallBar` +
  NineSlice-заливки), привязка к правому краю при resize (ловушка v8 с масками
  и UV — см. «UI-спрайты»); стартовое меню: фон `start.png`, кнопки `button.png`
  (Продолжить/Новая игра/Настройки/Выход), панель полного экрана.
- **2026-09-16** — квесты: `data/quests.js` (цепочка «Путь вожака стаи», 10
  заданий) + `modules/quests.js` (трекер, журнал по L, объявления); combat.js
  получил колбэк `onKill` для событий убийств героем; тик квестов — только в
  игровой сцене. Подробности — в разделе «Квесты».
- **2026-09-16** — перенос контента из образца forWork/example: `data/heroes.js`
  (4 класса), `data/abilities.js` (4×14 умений + иконки images/abilities),
  `data/enemies.js` (бестиарий 23 видов с особыми способностями); атаки и
  привязки видов выверены по образцу (dagger/rock — melee, club — бросок,
  cd атак — темп ИИ); активированы stoneskin (орк), poison (паук), call
  (гоблин → ai.alert). Подробности — в разделе «Контент из образца».
- **2026-09-16** — способности героя: `modules/abilities.js` + `WOLF_ABILITIES`
  (Огненный шар / Пронзающий рывок / Мороз — адаптации образца), панель слотов
  1–3 с кулдаун-шторками, combat.applySlow. Подробности — в разделе
  «Способности героя».


## Известные ограничения и идеи на будущее

- **32 компонента максимум** (битовая маска Uint32). При исчерпании — две половины Uint32 или
  таблица соответствий.
- `ECS.nextId` — глобальный, а не на world: несколько миров будут конфликтовать по ID.
- При экстремальной плотности (в разы выше «ёмкости» мира по кругам) коллизии расталкивают
  юниты за границы мира — толчение позиций в collisionSystem не ограничено стенками.
  Для игровых плотностей не проявляется.
- Скорости столкновений (импульс отскока) остаются в «пикселях за кадр» — при смене dt-масштаба
  скоростей проверить collisionSystem.
- Анимации AnimatedSprite уже кадронезависимы (сам спрайт читает deltaTime тикера).
- Звук — синтез, файлы не подключены.
